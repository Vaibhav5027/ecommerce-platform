name CI Pipeline

on
  push
    branches [ main, develop ]
  pull_request

jobs
  build
    runs-on ubuntu-latest
    steps
      - name Checkout code
        uses actionscheckout@v3

      - name Run dummy step
        run echo ✅ CICD is connected! Workflow is running.
